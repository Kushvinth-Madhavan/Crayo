{
  "name": "crayo-project",
  "private": true,
  "workspaces": [
    "city-relocation-advisor"
  ],
  "scripts": {
    "dev": "cd city-relocation-advisor && npm run dev",
    "build": "cd city-relocation-advisor && npm run build",
    "start": "cd city-relocation-advisor && npm run start"
  }
}
